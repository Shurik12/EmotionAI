# Docker compose guide line

This folder contains docker compose yaml files for different types of running.
Copy content one of them to ../docker-compose.yaml
```bash
cp docker-compose-single.yml ../docker-compose.yaml
```